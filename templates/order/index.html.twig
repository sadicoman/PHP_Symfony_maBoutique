{% extends 'base.html.twig' %}

{% block title %}Ma commande !
{% endblock %}

{% block body %}
	<div class="container my-5 mb-5 small-container">
		<h1>Ma commande !</h1>
		{{ form_start(deliverForm, {'method': 'POST'}) }}
		{{ form_row(deliverForm.addresses) }}
		<a href="{{ path('app_account_address_form')}}" class="small">Ajouter une nouvelle adresse de livraison</a>
		<hr>
		{{ form_row(deliverForm.carriers) }}
		{{ form_row(deliverForm.submit) }}
		{{ form_end(deliverForm) }}
	</div>
{% endblock %}
